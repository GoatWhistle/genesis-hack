# frozen_string_literal: true

# Файл догадок: единственное место, где человек спорит с инструментом.
#
# Мы принципиально не прячем догадки внутри генерации. Всё, что инструмент
# понял про чужое API, выкладывается в mapping.yml словами, и любое решение
# можно переписать руками — следующий прогон уважает правку. Это же ответ на
# вопрос «а если инструмент ошибётся»: ошибётся видимо и поправимо.

require_relative "manifest/writer"
require_relative "manifest/reader"
