# frozen_string_literal: true

# Заглушка контракта заказчика — единая точка подключения.
#
# Сгенерированному сервису нужен `Provider::BaseService`, которого у нас нет:
# 3 сентября эксперт подтвердил, что настоящий базовый класс командам не
# выдадут. Здесь собрана наша реконструкция контракта по примеру из задания,
# чтобы результат генерации можно было запустить, прогнать через поддельный
# сервер и покрыть тестами.
#
# Подключается отдельной строкой, а не из `lib/rsocket.rb`, и это намеренно:
# файл объявляет `Operation` и `Provider` в верхнем пространстве имён, потому
# что так они называются у заказчика. Тянуть их в каждый прогон командной
# строки незачем — их требуют только сгенерированный код и проверяльщик.
require_relative "runtime/operation"
require_relative "runtime/http_client"
require_relative "runtime/provider/errors"
require_relative "runtime/provider/base_service"
