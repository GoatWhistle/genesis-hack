# Проверка feat/classifier-recognition

## Повторная проверка с локальным корпусом (после cc8473b)

Этот раздел обновляет выводы прежнего отчёта ниже. Корпус доступен в исходной
рабочей директории; эталоны не менялись. Два субагента независимо доработали связи
ресурсов и callback, основной агент проверил изменения и общие результаты.

Исправлены реальные регрессии:

- QuantumPay: `enqueue` не является именем ресурса callback, `halt` — действие
  отмены; восстановлены уведомление и отмена.
- Moov: конечное `cancellations` обозначает отмену выбранного перевода.
- OpenBanking CH: сохранены параметры выбора продукта в пути создания; операция
  статуса/отмены может добавлять идентификатор платежа, не теряя родителей пути.
- Mastercard: распознаётся составное действие `payment-status`, но разные
  продукты credit-transfer не связываются между собой.
- Synctera: callback общей регистрации допустим только при явном перечислении
  события выбранного ресурса в enum тела уведомления. Событие external-card-transfer
  не считается доказательством события wire.
- GOV.UK Pay: общий структурный veto `checkout_request` распознаёт сочетание
  `return_url` с capture/MOTO/cardholder-полями. Один URL или отдельное поле capture
  не запрещают выплату. Исключения по имени провайдера не добавлялись.

### Замер всех 71 описания

| Правильные назначения, включая отказы | 7ddb793 | cc8473b | После доработки |
| --- | --- | --- | --- |
| Создание | 65 | 67 | 68 |
| Статус | 61 | 60 | 61 |
| Callback | 65 | 69 | 71 |
| Отмена | 65 | 61 | 65 |

Прочитаны 71/71. Последний прогон: создание — 0 пропусков, 0 лишних назначений,
3 неверные операции; статус — 7 пропусков, 0 лишних, 3 неверные; callback — без
расхождений; отмена — 4 пропуска, 1 лишнее назначение, 1 неверная по эталону операция.
Все 13 описаний группы отказа остались без ролей. Это проверенный обучающий корпус,
а не доказательство точности на неизвестных API.

### Все изменения назначений относительно локальных снимков 7ddb793

| Провайдер / роль | Изменение и обоснование |
| --- | --- |
| PayPal / отмена | Отказ вместо `payouts_item_cancel`: создание пачки `POST /v1/payments/payouts` не равно отмене её элемента `POST /v1/payments/payouts-item/{payout_item_id}/cancel`. |
| Fire.com / статус | Отказ вместо статуса родительской пачки: создание добавляет перевод в `/v1/batches/{batchUuid}/banktransfers`, а старый статус читает `/v1/batches/{batchUuid}`. |
| Fire.com / отмена | `delete_bank_transfer_batch_payment` удаляет выбранный элемент; прежняя `cancel_batch_payment` удаляет всю пачку. Эталон смешивает уровни ресурса. |
| Plaid, Lithic / callback | Отказ вместо посторонней проверки signal / карточной авторизации. |
| Modern Treasury / создание | `create_payment_order` вместо async-варианта, совпадает с эталоном. |
| TrueLayer / callback | `webhook_payouts` вместо `webhook_payment_links`. |
| Fireblocks / создание | Создание payout вместо второго шага execute; совпадает с эталоном, но двухшаговое исполнение целиком этим не реализовано. |
| Synctera / статус | Отказ вместо `get_cash_transfer`: `/cash/{id}` не описывает выбранный `/external_cards/transfers`. |
| Mercado Pago / отмена | Отказ: выбранное создание `/v1/payouts` создаёт пачку, прежняя отмена `/v1/payouts/{payout_id}/transactions/{transaction_id}/cancel` относится к элементу. |
| Checkout.com / callback | Отказ вместо несвязанного BACS-уведомления. |
| Nordigen / статус | Отказ вместо `/api/v2/payments/creditors/{id}/`: это счёт кредитора, не платёж. |
| GOV.UK Pay / создание, статус, отмена | Корректный отказ вместо классификации приёма платежа как выплаты. |
| Trustly, Up / callback | Отказ вместо посторонних уведомлений. |

Сомнительные эталоны PayPal/Fire.com не переписаны под алгоритм. Их расхождения
остаются в сырых цифрах. Восстановление прежней связи ради метрики было бы ошибкой.
Это также не доказывает, что добавление элемента пачки уже исполняет выплату:
многошаговые сценарии требуют отдельной поддержки адаптера.

### Проверки кода и сборки

- Полный RSpec после всех правок: **381 пример, 0 ошибок** (seed 44150).
- RuboCop `--cache false` по восьми изменённым Ruby-файлам: без замечаний.
- `git diff --check`: без ошибок.
- Генерация и вызовы созданных адаптеров на localhost: NovaPay **28/28**,
  KassaBox **17/17**, NordBank **12/12**, SwiftPay **21/21**.
- Новые регрессионные тесты синтетические и отслеживаются Git; дополнительно
  17 проверок связей на реальных файлах `bench/` пропускаются, если корпуса нет.

Окружение: `LANG=en_US.UTF-8 RBENV_VERSION=3.3.12 BUNDLE_WITHOUT=classifiers`,
Bundler через `/Users/kaifarikman/.rbenv/shims/bundle`. Команда корпуса:
`bundle exec ruby bench/measure.rb bench/truth.yml bench/truth-new.yml`.

### Сквозная проверка всего корпуса: существенные ограничения генератора

Дополнительно каждое из 71 описания передано в
`Rsocket.builder(rules: Config::Importer.call, classifier: "rules", tester: true)`.
Файлы результатов не публиковались; запросы тестов идут к локальному StubServer,
а не к платёжным провайдерам. Наличие роли в эталоне не гарантирует, что она
обязательна в контракте: среди 46 отказов сборки есть ожидаемые отказы API без
выплат и описаний без обязательного статуса.

Найден и исправлен дополнительный дефект `Renderer::Literal`: комментарий
после `nil` скрывал разделительную запятую. Теперь TODO печатается перед полем.
Добавлены 8 тестов компиляции плоского/вложенного payload, сохранности полей и
предупреждений. Неизвестные обязательные поля по-прежнему обозначены как TODO;
исправление синтаксиса не делает их значения корректными.

| Результат сквозной проверки | До исправления печати TODO | После |
| --- | --- | --- |
| Локальные пробы прошли | 5 | 16 |
| Пробы не прошли | 20 | 9 |
| Сборка отклонена: нет обязательных ролей | 46 | 46 |

Оставшиеся 9 провалов, не устранённые этим набором исправлений:

| Провайдер | Обнаруженное ограничение |
| --- | --- |
| PayPal, Velo, Dwolla | Идентификатор создания не извлекается из ответа; у Velo также проблемы запроса отмены. |
| Stripe, Paystack | Параметры `{payout}` / `{code}` не заполняются идентификатором перевода. |
| Interledger | Идентификатор в виде URL неправильно подставляется в путь. |
| OpenBanking UK | Разбор ответа статуса падает на несовпадении ожидаемой структуры: String/Integer. |
| OpenBanking CH | Не заполнены параметры выбора service/product; вызов идёт не на описанный маршрут. |
| Square | Проверка сравнивает ожидаемый ID с ID из примера; нужно разбирать согласованность фикстуры и извлечения. |

Успешная проба на собственных фикстурах не проверяет все обязательные поля,
корректность выбора бизнес-сценария, реальную авторизацию или фактическое исполнение
платежа. Поэтому результат **16** не означает 16 готовых production-интеграций.

Точечные дефекты и новые необъяснённые регрессии классификации закрыты по доступным проверкам.
Это **не** заявление о полной корректности любого сгенерированного адаптера:
остались неоднозначные потоки Plaid/Synctera/Mercado Pago, пропущенные статусы,
многошаговое исполнение и ограничения генерации. Требуется отдельная оценка
покрытия нужных продуктовых сценариев перед обещанием production-готовности.
Коммиты, push, слияние и деплой в этом заходе не выполнялись.

---

Дата: 6 сентября 2026. Исходный HEAD: `91a6df171c6f3968f1474c0e3c0fcf02e1a50fc4`
(в task.md аудит указан для `7ddb793`). Ruby 3.3.12, Bundler 4.0.20.

Исправлены три дефекта, добавлено 28 регрессионных примеров в `spec/`.
Полный RSpec: **303/303 до изменений, 331/331 после**. Эталонные данные и
правила оценки ролей не менялись. Изменения локальные; слияние и деплой не выполнялись.

## Изменения

- Swagger 2.0: ссылки общих parameters/responses раскрываются в исходном документе
  до переноса body и response.schema в content. Параметры пути наследуются операцией;
  параметры операции перекрывают их по in/name. Сохранены inline-схемы и definitions.
- Статус/отмена: сравнивается полный ресурсный путь с положением родительских
  идентификаторов. Удаляются только конечное действие и идентификатор ресурса.
  Совпадающие operationId или поля ответа не перекрывают конфликт ресурсов.
  Сохранены REST, RPC create/get, cancel, abort, revoke/revocation.
- Callback: парсер сохраняет ссылку на родительскую операцию вложенного callback.
  Она приоритетнее имени webhook. Для самостоятельных событий проверяются слова
  ресурса в пути/имени/тегах и специфичные поля payload. Признаки другого платёжного
  потока исключают кандидата. При равных доказательствах роль остаётся неназначенной
  с объяснением. Без распознанного создания callback не назначается.

## Проверки

Окружение команд:

```sh
export PATH="/opt/homebrew/opt/ruby@3.3/bin:$PATH"
export LANG=en_US.UTF-8
export BUNDLE_PATH=vendor/bundle
export BUNDLE_WITHOUT=classifiers
bundle install
```

До изменений приложения:

```sh
bundle exec rspec --exclude-pattern '**/{swagger2_references,resource_regressions,callback_regressions}_spec.rb'
```

Названия исключений соответствуют первым версиям новых тестов, впоследствии
переименованным. Результат: 303 примера, 0 ошибок, seed 44506. В sandbox первый
запуск дал 12 ошибок из-за запрета bind(127.0.0.1); повтор с разрешёнными локальными
сокетами прошёл. Это ограничение окружения, а не дефект приложения.

Первый запуск 20 новых сценариев на исходном приложении дал 15 ошибок:
5 Swagger, 5 связи ресурсов, 5 выбора callback. После исправлений добавлены ещё
8 проверок границ и наследования; всего 28 новых примеров.

Итоговые команды:

```sh
bundle exec rspec
bundle exec rubocop --cache false app/boot.rb app/models/api_operation.rb \
  app/service/adapter_builder/classification/{classifier,coherence,callback_coherence}.rb \
  app/service/adapter_builder/parsing/{operation_parser,swagger2_normalizer}.rb \
  spec/service/adapter_builder/classification/{coherence_regressions,classifier_callbacks}_spec.rb \
  spec/service/adapter_builder/parsing/spec_parser_swagger2_references_spec.rb
git diff --check
```

Результат: 331 пример, 0 ошибок, seed 33976; RuboCop — 10 файлов без замечаний;
проверка diff — без ошибок. Полный RSpec выполнялся с разрешёнными localhost-сокетами.

## Назначения до/после

Доступны только четыре описания из `examples/`. Снимки получены отдельно через
SpecLoader → SpecParser → Classifier с контрактом space_payments и сравнены
по каждой роли и каждому провайдеру. Все 16 назначений совпали:

| Провайдер | create_request | fetch_status | process_callback | cancel_request |
| --- | --- | --- | --- | --- |
| kassabox | make_transfer | transfer_info | отказ | abort_transfer |
| nordbank | create_payment_order | get_payment_order | отказ | revoke_payment_order |
| novapay | create_payout | get_payout_status | payout_webhook | cancel_payout |
| swiftpay | submit_transfer | fetch_transfer | отказ | revoke_transfer |

Относительно существующих ожиданий этих примеров: новых ложных назначений,
пропусков или выборов неправильной операции нет. На промежуточной версии
проверки ресурса пропали abort_transfer и revoke_payment_order. Причина — отсутствие
abort/revocation в списке конечных действий; исправлено, оба назначения восстановлены.

Разбор изменений на синтетических контрпримерах:

| Категория | До | После |
| --- | --- | --- |
| Swagger shared body/response | nil вместо схемы | схема definitions раскрыта |
| Swagger path parameters | отсутствующая schema | тип сохранён; наследование и override проверены |
| Родитель/дочерний ресурс, batch/item, ACH/wire | ложная связь при совпадающих именах/схемах | отказ от связи |
| webhook_payment_links и webhook_payouts | выбрано чужое событие | выбран webhook_payouts |
| Приём платежа/карточная авторизация | ложное назначение callback | отказ |
| Два одинаково обоснованных payout-события | первое по порядку | безопасный отказ с объяснением |
| Одноимённые вложенные callbacks разных родителей | родитель утрачен, выбор по порядку | выбран callback создания выплаты |

Отказ при двух payout-событиях может быть пропуском полезного callback. Это
сознательное ограничение: без дополнительного доказательства выбрать единственную
операцию нельзя. Синтетический пример с именами TrueLayer не заменяет проверку
на самом описании TrueLayer.

## Ограничения и рекомендация

`bench/`, оба корпуса, truth.yml/truth-new.yml и measure.rb отсутствуют в свежем
клоне. Команда `bundle exec ruby bench/measure.rb bench/truth.yml bench/truth-new.yml`
недоступна. Сравнение 71 описания и подробная проверка каждого провайдера этих
корпусов **не выполнены**; исходные числа из task.md не выдаются за наш замер.

Известное ложное назначение выплаты у GOV.UK Pay не исправлялось и не перепроверялось:
описания нет. Это остаётся известным дефектом, а не «продуктовой неоднозначностью».
Группа holdout уже использовалась при настройке и не является независимой проверкой.

Структурная связь намеренно консервативна: нестандартные RPC-адреса и разные
наименования одного потока могут остаться без роли. Для callback неполные имена,
неизвестные синонимы, неоднозначные события и недостаточная схема дают отказ.
LLM, embeddings, внешних вызовов классификатора и исключений по провайдерам нет.

Исправление Swagger готово к отдельному review/слиянию по доступным проверкам.
**Слияние всей ветки пока не рекомендовано**: остаётся известный GOV.UK Pay и
нет повторного замера обоих корпусов. Перед общим слиянием нужен этот замер с
разбором изменившихся назначений. Успешные unit-тесты и тесты с локальным сервером
не доказывают работоспособность реальной интеграции с платёжным провайдером.

Локальные логи: tmp/rspec-before-unrestricted.log, tmp/regressions-before.log,
tmp/rspec-after.log, tmp/rubocop-final.log, tmp/before.json, tmp/after.json.
Каталог tmp игнорируется Git.
